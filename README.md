
2019年1月6日 22:17:25
apm.db中创建表的语句：
CREATE TABLE IF NOT EXISTS "uandp"(
	     bbs_name text primary key,
		 username text,
		 passwd text,
		 email text,
		 detail text);

2019年1月4日 16:36:52
commit 1
之前的代码看不懂了，
重新设计重新写，
先实现基础的功能，
后面的优化后面再改；
